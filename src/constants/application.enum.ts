export enum ApplicationStatusEnum {
  pending = 'pending',
  approved = 'approved',
  rejected = 'rejected',
  incomplete = 'incomplete',
  withdrawn = 'withdrawn',
  closed = 'closed',
  cancelled = 'cancelled',
  submitted = 'submitted',
}
